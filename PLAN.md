# Plan de accesibilidad — Fear & Hunger 1

## Fase 1 — Base funcional

- [x] **1.1** Descargar `ScreenReaderAccess.js` del repo [craigbrett17/rpgmaker-mv-access](https://github.com/craigbrett17/rpgmaker-mv-access)
- [x] **1.2** Copiar el plugin a `www/js/plugins/`
- [x] **1.3** Registrar el plugin en `www/js/plugins.js` **después** de `YEP_MessageCore` (orden de carga crítico)
- [x] **1.4** Lanzar el juego con NVDA activo y verificar que los diálogos se leen en voz alta. Verificado.
- [x] **1.5** Verificar que el menú principal (opciones, nuevo juego, continuar) se anuncia correctamente. YEP_OptionsCore: ampliado el hook de `Window_Options.select` para leer también el `HelpDesc` de cada opción y categoría (bitmap-only en pantalla); verificado en esta sesión.

## Fase 2 — GabWindow

- [ ] **2.1** Identificar en qué momentos del juego aparece el GabWindow (mensajes flotantes de NPC / sistema)
- [x] **2.2** Escribir hook sobre `Window_Gab.prototype.drawGabText` para capturar el texto y enviarlo a la región `aria-live`. Ya implementado en `ScreenReaderAccess.js` tras guardia `Imported.YEP_GabWindow`.
- [x] **2.3** Verificar que NVDA lee los mensajes flotantes en el dungeon. Confirmado: "An unnatural hunger begins draining your strength..." desapareció solo — es GabWindow, no Window_Message. El hook de `drawGabText` lo capturó correctamente.

## Fase 3 — Menús custom

- [x] **3.1** Hookear `GALV_BustMenu` para que NVDA lea nombres de actores, HP/MP y stats al navegar el menú principal. `Window_MenuStatus` no define `select` propio (hereda de `Window_Selectable`) y GALV navega con `cursorUp/Down` → `this.select()`; se hookea `Window_MenuStatus.prototype.select` para anunciar nombre, nivel, clase, HP, MP y estados (espejo de `drawActorSimpleStatus`). Las etiquetas HP/MP usan `TextManager.hp`/`TextManager.mp` para respetar los nombres del juego (Body/Mind). **Fix (hambre):** el readout omitía Hunger (visible en el panel del menú/equipo junto a Body/Mind). F&H reutiliza el **EXP del actor** como medidor de hambre: los common events `HUNGER_*` espejan `actor.EXP` en variables y disparan los estados de inanición a partir de él, y `GALV_BustMenu` dibuja el gauge de EXP renombrado "Hunger" (param `Exp Text`) encima de las barras, imprimiendo `actor.nextRequiredExp()` (EXP restante dentro de la banda del nivel, p. ej. `157 - currentExp` para Dark Priest nivel 2; el comentario "58 + EXP" del CE = `expForLevel(2)≈57`). Se añade `describeHunger()` que mirrorea ese valor exacto (label tomado de `Galv.BM.xpLabel`, guardado contra ausencia del bust menu y contra nivel máximo) y se inserta antes de Body para reflejar el orden visual del panel. Pendiente de verificación con NVDA.
- [x] **3.2** Hookear `AltMenuScreen` para que se anuncien las opciones del menú (Objetos, Habilidades, Equipo, Estado, Guardar, Salir). Cubierto por el hook genérico existente `Window_Command.prototype.select`: `AltMenuScreen` solo reordena el layout y `Window_MenuCommand` hereda `select` de `Window_Command`. Verificar con NVDA en 3.5.
- [x] **3.3** Hookear `AltSaveScreen` para que se lean los slots de guardado y su información. `Window_SavefileList` hereda `select` de `Window_Selectable` (no de `Window_Command`), por eso el hook genérico no lo cubría; se hookea `Window_SavefileList.prototype.select` para anunciar número de archivo + título + tiempo de juego (o "Empty"), espejo de `Window_SavefileStatus.drawContents`. Verificado con NVDA.
- [x] **3.4** Pantalla de selección de personaje (New Game, `Map010`): hookear `Game_Picture.prototype.show` para anunciar nombre + descripción de cada clase (Mercenary, Knight, Dark Priest, Outlander). La pantalla es 100% imágenes (`text_<clase>.rpgmvp` en Picture ID 7), sin texto de Window; descripciones transcritas verbatim de las imágenes y mapeadas en el plugin.
- [x] **3.5** Verificar navegación completa de menús sin vista. Cubierto y verificado: síntesis (`YEP_ItemSynthesis` — `Window_SynthesisStatus.prototype.refresh` para el panel de estadísticas y `Window_SynthesisList.prototype.select` para las recetas) y la expansión de nombres abreviados del menú ("Q"/"Sk"/"Eq"/"St" → "Quit"/"Skills"/"Equipment"/"Status") en el hook de `Window_Command.prototype.select`. Pendiente de verificar: pantalla de equipo (`Scene_Equip`) — `Window_EquipSlot.prototype.select` anuncia slot + ítem equipado + descripción; `Window_EquipItem.prototype.select` anuncia ítem + descripción + cambio de atributos (diff de actor temporal espejo de `Window_EquipItem.updateHelp`, params 2..7 = Attack..Luck). El panel de atributos (`Window_EquipStatus`) es solo dibujo y nunca recibe foco (en F&H solo funcionan left/right sobre Equip/Optimize/Clear), así que los 6 atributos actuales se leen a demanda con **Tab** (handler global; funciona en cualquier escena con `actor()`: Equip/Status/Skill). Optimize/Clear ahora anuncian la acción + atributos resultantes (antes solo sonaban, parecían no hacer nada).

## Fase 4 — Combate

**Corrección de alcance (verificado en `plugins.js`):** F&H tiene OctoBattle activo pero con casi todos sus subsistemas **apagados** — `Order Turn Battle:false` (usa `dtb`), `Break Shield:false`, `Boost Point:false`, `Weapon Swap:false`, `Victory Screen UI:false`, `Weakness Display:false`. El combate real es **YEP_BattleEngineCore + ventanas por defecto + enemigos-por-parte** (cada parte del cuerpo es un `Game_Enemy` separado con su propio HP, p. ej. "head 20/20"). Por eso 4.4–4.7 quedan como **N/A en F&H**.

- [x] **4.1** `Window_ActorCommand` — anunciar cada comando al navegar (Attack, Skill, Item, Guard…). Cubierto por el hook genérico `Window_Command.prototype.select` (`Window_ActorCommand` hereda `select`; OctoBattle solo redefine `processCursorMove` para weapon-swap, desactivado aquí). Confirmado por el usuario que la navegación se lee. Pendiente de re-verificar con NVDA tras los cambios de esta sesión.
- [x] **4.2** `Window_BattleEnemy` — anunciar la parte objetivo. El hook existente ya daba "nombre: hp/mhp"; **ampliado** `Window_BattleEnemy.prototype.select` (y `Window_BattleActor.select`) con `describeBattlerStates()` para añadir los estados activos de la parte (p. ej. miembro cercenado/sangrado), que es la única info extra en pantalla con el Weakness Display apagado. Pendiente de verificar con NVDA.
- [x] **4.3** Anuncio de daño/curación. **Bug de raíz corregido:** los re-emit de HP/MP/TP/estados se gateaban con `if (!Yanfly.Param.BECShowHpText)`, pero Yanfly guarda esos flags como *strings* `"true"/"false"` y los lee con `eval()`; `!"false"` es `false`, así que los hooks **nunca se instalaban** en F&H (que apaga todos los textos del log) → el daño era silencioso. Añadido `becShowsText()` (evalúa el flag como Yanfly) y migradas las 4 guardas HP/MP/TP/State a usarlo. Pendiente de verificar con NVDA.
- [x] **4.3b** Texto de acción + resultado, antes suprimidos por `Show Action/Critical/Miss/Evasion/Fail Text:false`: nuevo hook `Window_BattleLog.displayAction` anuncia "`<actor>: <acción>`" (la ruta "simple" de Yanfly soltaba el nombre del actor); `addText` ahora salta la línea `<SIMPLE>` para no duplicar el nombre de la habilidad. Re-emitidos también `displayCritical`/`displayMiss`/`displayEvasion`/`displayFailure` con los strings de `TextManager`. Pendiente de verificar con NVDA.
- [~] **4.4** Break Shield System — **N/A en F&H** (`Break Shield:false`).
- [~] **4.5** Boost Point System — **N/A en F&H** (`Boost Point:false`).
- [~] **4.6** Order Turn Battle — **N/A en F&H** (usa `dtb`, turno por defecto).
- [~] **4.7** Weapon Swap — **N/A en F&H** (`Weapon Swap:false`).
- [x] **4.8** Victory Sequence. **Hallazgo (verificado contra los datos):** F&H **no da recompensas estándar** — los 600 enemigos de `Enemies.json` tienen `exp:0`, `gold:0` y **sin drops**, y no hay comandos de cambiar exp/oro en eventos de tropa (`315`/`125` = 0). Por eso `BattleManager.displayExp/displayGold/displayDropItems` no añaden nada y "`<nombre> was victorious!`" (que sí se lee por el hook de `Window_Message.startMessage`, ruta confirmada: con `Victory Screen UI:false` el bloque OctoBattle gateado en `if (Olivia.OctoBattle.VictoryUI.Enabled)` se salta, así que corre el `processVictory` de Yanfly → default, que mete la victoria por `$gameMessage`) es **toda** la salida de victoria por defecto. No subir de nivel en combate (exp 0). **El verdadero hueco:** el botín se entrega por **eventos de tropa** con "Cambiar objetos/armas/armaduras" (códigos `126`/`127`/`128`), **mudos en vanilla** — 197 ganancias de loot sin texto adyacente vs solo 4 con texto (p.ej. matar a *Guard1* da *Silver coin* + *Meat cleaver* en silencio). **Fix:** se hookean `Game_Interpreter.command126/127/128` con `announceBattleItemGain()`, que recalcula `operateValue(op, operandType, operand)` tras correr el original (lee los mismos params/variable, valor idéntico), anuncia "Received `<nombre>`" (con cantidad si >1) y se **gatea con `$gameParty.inBattle()`** para no tocar cofres del mapa (que ya traen su propio Show Text) ni el uso de objetos. Verificado con NVDA.
- [x] **4.9** `Window_BattleStatus` — lectura a demanda del HP/estado de la party durante el combate (una tecla, estilo el Tab del menú de equipo) en vez de espamear cada redibujado. Implementado reutilizando el handler global de **Tab** (keyCode 9): cuando `SceneManager._scene instanceof Scene_Battle`, anuncia el estado de toda la party con el nuevo `describePartyBattleStatus()`, que itera `$gameParty.battleMembers()` y lee, por actor, nombre + HP `x of y` + MP `x of y` (etiquetas `TextManager.hp`/`mp`, respetando Body/Mind) + estados activos (reusa `describeBattlerStates()`). Versión compacta del readout del menú (3.1): omite nivel/clase/hambre por ser contexto solo-menú y deja lo relevante en combate. `Window_BattleStatus` es draw-only y nunca recibe foco, así que esta es la única forma de oír la party sin espamear en cada redibujado. **Lado enemigo (Shift+Tab):** F&H es front-view — los enemigos son solo sprites sin panel de estado persistente (Weakness Display de OctoBattle apagado, sin plugin de barras de HP), así que la única info en pantalla era la lista de selección de objetivo (`Window_BattleEnemy.select`, ya cubierta). El dato sí está expuesto vía `$gameTroop`, así que `describeEnemiesBattleStatus()` recorre `$gameTroop.aliveMembers()` y lee nombre + HP `x of y` + estados de cada enemigo vivo (cada parte del cuerpo de F&H es un miembro separado, p.ej. "head: 20 of 20. torso: 35 of 35"). Se enchufa en el mismo handler de Tab diferenciando por `event.shiftKey`. Verificado con NVDA.

## Fase 5 — Navegación por el mapa

- [x] **5.1** Verificar que `WallBump.js` está instalado y funciona (sonido al chocar con paredes). Verificado.
- [x] **5.2** Verificar que `InteractableElementsMenu.js` funciona (hotkey que lista objetos interactuables). La lista ya se lee con NVDA (cubierta por el hook genérico `Window_Command.prototype.select`; se abre con **Shift+I** porque el listener usa el evento `keypress` y el keyCode 73 = 'I' mayúscula). Reescrito el seguimiento sonoro: en vez de modular el BGM (que no sonaba si el mapa no tenía música y solo cambiaba al moverse), ahora `updateBeacon()` reproduce un SE dedicado en bucle como baliza de audio — **pan** = desplazamiento horizontal, **pitch** = vertical (arriba más agudo), **intervalo de repetición + volumen** = distancia (cerca = rápido/fuerte). Usa coords del evento en vivo, cancela al cambiar de mapa y reproduce un SE de llegada al alcanzar el tile. Parámetros nuevos: `Beacon Sound` (def. Cursor1), `Arrival Sound` (def. Bell1). Pendiente de verificación con NVDA en el juego.
- [x] **5.6** Nombrar los eventos del menú de interactuables. Antes la lista mostraba "Event N at x,y" porque usaba `element._name` (inexistente en `Game_Event`; los nombres del editor de F&H son autogenerados `EVxxx`) y los objetos interactuables suelen ser **eventos invisibles sin sprite** (un trigger de acción sobre el dibujo del parallax: barril, caja, cadáver). La única identidad legible es el texto que muestra el evento al usarse, así que se añade `deriveEventLabel()`: escanea la página activa (`event.list()`, protegido contra `_pageIndex === -1`) y luego todas las páginas (`event.event().pages`) buscando la primera línea de "Mostrar texto" (código 401 no vacío); si no hay texto, recurre al nombre del sprite limpiando los prefijos `!$` y los `_` ("$seed_mercenary" → "seed mercenary"); si tampoco, el llamador añade el genérico "Event N". El texto se sanea con `cleanLabel()` (quita `\c[n]`/`<WordWrap>`/etc. y trunca a 60). Ahora "Event 49" se lee "A dead horse is laying down here. at 9, 16", barriles/cajas se nombran solos, etc. Verificado contra los datos reales de Map074/Map131. Además, la lista se **ordena por cercanía** al jugador (distancia Manhattan, los más cercanos primero) en lugar de por `_eventId`; empate desempatado por id para orden estable. Pendiente de verificación con NVDA en el juego.
- [x] **5.5** Escáner de salidas (`ExitScanner.js`). Nuevo plugin: la tecla **E** (keyCode 69, libre en `Input.keyMapper`) anuncia las cuatro direcciones cardinales y cuántas casillas hay libres hasta toparse con pared, p. ej. "North 5. East 1. South blocked. West blocked." (o "All directions blocked." en callejón sin salida). Lee la transitabilidad directamente del motor con `Game_Player.canPass` recorriendo tile a tile con `roundXWithDirection`/`roundYWithDirection`, así que es exacto sin depender de la iluminación. Da información **proactiva** del camino sin tener que chocar (complementa el `WallBump` reactivo). Para anunciar sin duplicar lógica, se expuso `window.ScreenReaderAccess.announce(message, interrupt)` en `ScreenReaderAccess.js`, que enruta por el mismo `sanitize` + región aria-live (asertiva). Registrado en `plugins.js` tras los demás de accesibilidad. Pendiente de verificación con NVDA.
- [x] **5.7** Radar direccional (`Radar.js`). Nuevo plugin: la tecla **R** (keyCode 82, libre en `Input.keyMapper`) activa/desactiva un radar. Mientras está activo, cada vez que el jugador **gira** o **da un paso** se hookea `Game_Player.prototype.moveStraight` para escanear hacia la dirección encarada con `scanAhead()`: recorre tile a tile (hasta `Max Scan`, def. 12) con `canPass`/`roundXWithDirection` y devuelve el primer obstáculo, clasificándolo como **muro** o **interactuable** (reutiliza la detección `eventsXy` + `isTriggerIn([0,1,2])` de WallBump/InteractableElementsMenu, con manejo de tiles `isCounter`). Reproduce un SE distinto según el tipo (`Wall Sound` def. Earth3, `Interactable Sound` def. Saint5) y codifica la **dirección** igual que el beacon — pitch alto=arriba / bajo=abajo, pan izquierda/derecha — y la **distancia** en el volumen (cerca=fuerte). Cubre "ambos" disparos: los pasos a tiles nuevos (`isMovementSucceeded`) siempre suenan; los choques/giros contra muro se limitan con `Bump Debounce` (def. 30 frames, decrementado en `Game_Player.update`) para no ametrallar al mantener la tecla. El on/off se anuncia con `window.ScreenReaderAccess.announce`. Complementa el WallBump reactivo dando aviso **proactivo y con alcance** de lo que viene. Registrado en `plugins.js` tras los demás de accesibilidad. Pendiente de verificación con NVDA en el juego.
- [x] **5.8** Selección rápida de interactuables sin abrir el menú (teclas **A** / **S**, keyCodes 65/83, libres en `Input.keyMapper`). Para dar fluidez, en `InteractableElementsMenu.js` un cursor (`quickSelectIndex`) recorre la **misma lista ordenada por cercanía** que el menú (refactorizada a `sortedInteractableElements()` y compartida). Un listener `keydown` (con guardas `Scene_Map` + `!isEventRunning`, igual que Radar) llama a `quickSelect(±1)`: **S** = siguiente/más lejos, **A** = anterior/más cerca, con clamp a los extremos. El cursor se **reinicia a 0 en cada paso** del jugador (hook `Game_Player.prototype.increaseSteps`), de modo que tras caminar la primera **A** anuncia el primer evento (el más cercano) y la primera **S** el segundo; estando quieto, A/S avanzan/retroceden por la lista. Cada selección **anuncia** la descripción vía `window.ScreenReaderAccess.announce` (texto extraído a `describeElement()`, compartido con el menú) y **arranca el beacon de inmediato** (`startTrackingElement()` reusa el seguimiento sonoro existente, `beaconTimer=9999` para sonar al instante). `quickSelectMapId` evita arrastrar un índice obsoleto entre mapas. Pendiente de verificación con NVDA en el juego.
- [x] **5.9** Detección de trampas de suelo (`TrapWarning.js`). Nuevo plugin. Las trampas de F&H son **eventos de mapa** que ninguna herramienta existente detectaba: los `bearTrap*` usan sprite `$beartrap` con disparo **Proceso paralelo** (trigger 4) y los `gas_trap*` son **invisibles** (sin sprite) con disparo **Contacto con jugador** (trigger 1); ambos son **prioridad 0** ("debajo de personajes"), así que `isInteractable()` los rechaza (exige `isNormalPriority()`) y nunca llegaban al menú de interactuables, la selección rápida A/S ni el Radar. Como el tile es transitable (`through:true`), `ExitScanner`/`WallBump` lo trataban como suelo libre → el jugador ciego no recibía ninguna señal. Detección en runtime sin coordenadas fijas: trampa = evento cuyo `event().name` casa con `/trap/i`; se considera **desarmada** si su self-switch A está activo (los cepos disparados activan A y cambian a una página inofensiva; los gas no tienen self-switch → siempre armados). Dos vías, **solo aviso, sin bloquear el movimiento** (decisión del usuario): (1) **escaneo manual** con **T** (keyCode 84) que lee las trampas armadas en radio `Max Scan` (def. 8), las más cercanas primero, con el mismo formato de desplazamiento del menú ("Trap, 2 down 1 left. Trap, 3 right.") o "No traps nearby."; (2) **aviso por paso**: tras cada paso (hook `Game_Player.increaseSteps`) si una trampa armada queda en uno de los cuatro tiles cardinales adyacentes (donde se podría pisar al siguiente paso) se anuncia una sola vez ("Trap north.") con un SE `Danger Sound` (def. Buzzer1); se rastrean los ids adyacentes para no repetir el aviso de la misma trampa al recorrer un pasillo de cepos. Registrado en `plugins.js` tras los demás de accesibilidad. Pendiente de verificación con NVDA en el juego.
- [x] **5.10** Sonar de enemigos en el mapa (`EnemySonar.js`). Nuevo plugin. Los enemigos de F&H son `Game_Event` que inician combate por contacto y se distinguen del resto de interactuables porque su página activa contiene un comando **"Battle Processing"** (código 301); los enemigos abatidos cambian a una página sin ese comando (self-switch) y dejan de detectarse automáticamente. A diferencia de Radar (5.7) o el beacon (5.2), un enemigo es un evento **en movimiento** y una amenaza constante, así que el sonar está **siempre activo, sin tecla de on/off**: cada frame (hook `Scene_Map.prototype.update`, gateado contra `isEventRunning`/`$gameMessage.isBusy`) recorre `enemyEvents()` y, por cada enemigo vivo, reproduce un SE posicional (`Enemy Sound`, def. `Monster1`). Codificación espacial igual que el beacon: **pan** = desplazamiento horizontal (muy a la derecha → pan fuerte a la derecha; a un paso → casi centrado, `dx/10*100` clamped a ±100), **pitch** = desplazamiento vertical (muy arriba → agudo, muy abajo → grave, apenas arriba → ligeramente agudo, `100 + (-dy/10*50)` clamped), **volumen** = distancia (cerca fuerte, lejos tenue). Cadencia por enemigo con timer propio (`pingTimers` keyed por `_eventId`, reseteado al cambiar de mapa y purgado de enemigos muertos/ausentes): un ping **cada segundo** (60 frames) y **cada medio segundo** (30 frames) cuando el enemigo está a **≤5 pasos** (distancia Manhattan, `Near Threshold`). Sin habla — es señal puramente espacial y continua para localizar y esquivar enemigos como hace el vidente al verlos moverse. **Dos filtros para no delatar enemigos que el jugador no podría percibir** (añadidos tras detectar en pruebas que se oían enemigos de salas contiguas): (1) **`Max Range`** (def. 10 tiles Manhattan; 0 = ilimitado) silencia a los lejanos; (2) **`Line Of Sight`** (def. on) traza una recta Bresenham del jugador al enemigo y lo silencia si un tile-muro queda en medio — "muro" = tile impasable en las cuatro direcciones (`!$gameMap.checkPassage(x,y,0x0f)`), leído de los flags de paso del mapa, así que es exacto e independiente de la iluminación. Al quedar fuera de rango o tras un muro se borra su timer para que vuelva a pingar de inmediato al reaparecer en vista. Registrado en `plugins.js` tras los demás de accesibilidad. Pendiente de verificación con NVDA en el juego.
- [ ] **5.3** Añadir anuncio de nombre de mapa/área al hacer transición entre mapas
- [ ] **5.4** Evaluar si el sistema de iluminación (`TerraxLighting`) requiere algún anuncio de estado (zona oscura, fuente de luz)

## Fase 6 — Pulido y distribución

- [ ] **6.1** Revisar escenas especiales del juego que usen UI custom no cubierta por las fases anteriores
- [ ] **6.2** Añadir opción en el menú de opciones para activar/desactivar el modo accesibilidad
- [ ] **6.3** Empaquetar todo como mod de [FHMM](https://github.com/mattieFM/FearAndHungerModManager) (archivo `.js` + `.json` de metadatos)
- [ ] **6.4** Publicar en audiogames.net y buscar testers ciegos reales
- [ ] **6.5** Iterar con feedback de testers

## Notas técnicas

- **Región aria-live**: div oculto en el DOM que NVDA/JAWS leen automáticamente. No requiere Tolk.
- **Orden de carga en plugins.js**: nuestro plugin siempre al final, después de YEP_MessageCore y OctoBattle.
- **NW.js / Chromium**: versión del juego ≥ Chromium 69. La técnica aria-live funciona sin actualizar el runtime.
- **Mouse desactivado**: F&H ya es 100% teclado. La navegación es predecible.
